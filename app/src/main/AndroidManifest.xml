<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pleb.decisionsandroid">


    <!-- Backup and restore -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewList"
            android:label="New Question" >

            <!-- For icon up navigation -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.pleb.decisionsandroid.NewList" />
        </activity>
        <activity
            android:name=".Decision"
            android:label="Decision" >

            <!-- For icon up navigation -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.pleb.decisionsandroid.Decision" />
        </activity>
        <activity
            android:name=".EditDecision"
            android:label="Edit decision" >

            <!-- For icon up navigation -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.pleb.decisionsandroid.EditDecision" />
        </activity>

    </application>

</manifest>